import { CredentialOperationEntity } from './credential-operation.entity';

describe('CredentialOperationEntity', () => {
  it('should be defined', () => {
    expect(new CredentialOperationEntity()).toBeDefined();
  });
});
