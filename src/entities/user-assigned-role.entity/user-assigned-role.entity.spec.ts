import { UserAssignedRoleEntity } from './user-assigned-role.entity';

describe('UserAssignedRoleEntity', () => {
  it('should be defined', () => {
    expect(new UserAssignedRoleEntity()).toBeDefined();
  });
});
